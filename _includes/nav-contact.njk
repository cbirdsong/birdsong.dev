<nav class="nav nav-horizontal" id="nav-contact" aria-label="Contact">
  <h2 class="nav__title">Contact:</h2>
  <ul>
    {%- for item in contact %}
      <li>
        <a href="{{ item.href | url }}" data-print-label="{{ item.value }}" aria-label="{{ item.title }}: {{ item.value }}">
          {{ item.title }}
        </a>
      </li>
    {%- endfor %}
  </ul>
</nav>